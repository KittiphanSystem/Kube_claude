# Auto-generated by blueprintctl
apiVersion: v1
kind: LimitRange
metadata:
  name: tenant-demo-tenant-limits
  namespace: tenant-demo-tenant
  labels:
    kube-saas/tenant: "demo-tenant"
spec:
  limits:
    - type: Container
      default:
        cpu: 500m
        memory: 512Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      max:
        cpu: "1"
        memory: 2Gi
      min:
        cpu: "10m"
        memory: "16Mi"
    - type: Pod
      max:
        cpu: "2"
        memory: 4Gi
    - type: PersistentVolumeClaim
      max:
        storage: 20Gi
      min:
        storage: "1Gi"
